import { StyleSheet } from "react-native";

export const shadow = StyleSheet.create({
  cardShadow: {
    shadowColor: "#111",
    shadowOffset: {
      width: 0,
      height: 8,
    },
    shadowOpacity: 0.43,
    shadowRadius: 9,

    elevation: 10,
  },
});
